name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    env:
      # 为空则构建产物内反馈通道为未配置态（fork PR 无权读取 secret，属预期行为）
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 8.0.x

      - name: Publish loose-file version
        run: dotnet publish SteamLuaManager.csproj -c Release -r win-x64 --self-contained false -p:DebugType=none -o publish/loose

      - name: Publish single-file version
        run: dotnet publish SteamLuaManager.csproj -c Release -r win-x64 --self-contained false -p:DebugType=none -p:AssemblyName="Fluent Steam Lua" -p:PublishSingleFile=true -o publish/single

      - name: Upload loose-file artifact
        uses: actions/upload-artifact@v5
        with:
          name: loose-file
          path: publish/loose/
          retention-days: 7

      - name: Upload single-file artifact
        uses: actions/upload-artifact@v5
        with:
          name: single-file
          path: publish/single/
          retention-days: 7
